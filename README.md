# k8s-42-session-setup
This script allow you to install all necessary tools for ft-services project in goinfre without running out of space 
#
Check first if VirtualBox is installed in your session
run:
```./setup.sh```
